// This is a JavaScript file

// Get the main app.js module and inject the SharedProperties service
var transformerHeader = angular.module("transformerHeaderController", []);
transformerHeader.controller("TransformerHeaderController", function($scope, $http, SharedProperties)
{
    
});